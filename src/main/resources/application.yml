spring:
  application:
    name: meanhwa_back
  profiles:
    default: local

server:
  port: 8080

app:
  jwt:
    secret: default-dev-jwt-secret-for-meanhwa-local-and-test-only
    access-token-validity-minutes: 30
    refresh-token-validity-days: 14
